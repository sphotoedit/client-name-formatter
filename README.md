# client-name-formatter
Client-Name-Formatter A simple HTML tool that generates a string like 2025_17 Aug_Client Name using the current date and a user-provided client name, then copies it to the clipboard. Built with HTML, CSS, and JavaScript.
